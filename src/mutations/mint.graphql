mutation MintDrop($drop: String!) {
  mint(drop: $drop) {
    address
  }
}

mutation TransferMint($id: String!, $wallet: String!) {
  transferMint(id: $id, wallet: $wallet) {
    address
    owner
  }
}
